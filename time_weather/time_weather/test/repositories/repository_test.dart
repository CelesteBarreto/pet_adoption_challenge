/* import 'package:flutter_test/flutter_test.dart';
import 'package:mockito/annotations.dart';
import 'package:mockito/mockito.dart';
import 'package:time_weather/repositories/repository.dart';
import 'repository_test.mocks.dart';

class WeatherRepositoryMock extends Mock implements WeatherRepository {}

@GenerateMocks([WeatherRepositoryMock])
void main() {
  setUp(() {
    var repository = MockWeatherRepositoryMock();
  });

/*   test('Retorna os dados do tempo', () {
    when(() {});
    //act
    //expect
  }); */
}
 */